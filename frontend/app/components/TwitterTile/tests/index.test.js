// import React from 'react';
// import { shallow } from 'enzyme';

// import InstagramTile from '../index';

describe('<InstagramTile />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
