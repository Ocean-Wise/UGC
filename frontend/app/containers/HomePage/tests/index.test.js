/**
 * Test the HomePage
 */

describe('<HomePage />', () => {
  describe('mapDispatchToProps', () => {
    it('Expect to have unit tests specified', () => {
      expect(true).toEqual(true);
    });
  });
});
